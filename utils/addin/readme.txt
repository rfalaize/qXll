# qWrapper

Description:
Encapsulates the view logic and interacts with excel objects (worksheets, ranges).
Note: Modules are loaded dynamically when opening the add-in, so that they can be commited, version controlled... etc.